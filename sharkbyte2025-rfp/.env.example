# Environment configuration for SharkByte 2025 RFP Web App (MERN)
# Copy this file to `.env` and fill in the values.

# Server configuration
PORT=3000
# If serving a separate React client, set CORS_ORIGIN to that URL (e.g., http://localhost:5173)
CORS_ORIGIN=http://localhost:3000

# MongoDB (MERN persistence)
# Example local: mongodb://127.0.0.1:27017/sharkbyte_rfp
# Example Atlas: mongodb+srv://<user>:<pass>@<cluster>/<db>?retryWrites=true&w=majority
MONGO_URI=

# Gemini API configuration
# We use Google Gemini to assist with generating keywords for proposals.
# Obtain an API key from: https://ai.google.dev/
# NEVER commit your real key to version control.
GEMINI_API_KEY=your_gemini_api_key_here
# Optional: override model; defaults to gemini-1.5-flash
GEMINI_MODEL=gemini-1.5-flash

# Security note: The backend will redact PII (names, emails, phone numbers,
# addresses, and likely company identifiers) from RFP content BEFORE sending
# any text to Gemini. Do not include sensitive info in the project goal/summary
# inputs you provide to the /api/keywords endpoint.
